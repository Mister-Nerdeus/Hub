Current-gap evidence captured before implementation:

The web app loaded converted JSON default plan fixtures for render and route proof modules, but it had no floorplan-first library view model or library component. Normal app startup therefore did not list the five default JSON floorplans as read-only library records, and there was no web-level proof that library data excluded DOCX filenames, paths, preview links, or download links.
