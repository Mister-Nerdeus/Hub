Current-gap evidence before implementation:

- `App.tsx` rendered simulation retrieval, assignment, reports, route preview, comparison, bundle audit, optimizer, plan builder, raw JSON, save/load, import/export, and renderer proof modules in the normal workflow.
- The API base URL pill was visible in the normal workflow.
- No Developer Proof Mode state or toggle existed.
