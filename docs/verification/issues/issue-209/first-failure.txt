Issue 209 current gap:

Before this issue, the default plan import foundation had a source manifest but no explicit source-to-plan mapping contract and no mapping skeletons. Conversion from visual DOCX reference labels to structured plan object IDs would have been subjective and hard to audit.

Default plan conversion remained NO-GO before mapping contract creation.
