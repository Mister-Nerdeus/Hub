First failure reproduced before implementation:

Command:
node scripts/check-floorplan-authoring.mjs --stage save-records --allow-partial --issue 271

Observed:
MODULE_NOT_FOUND for scripts/check-floorplan-authoring.mjs.

Root cause:
The repository had no floorplan authoring gate and the saved floorplan store was still an in-memory single-ID store using saved-${plan.planId}, so Save As and durable reload could not be proven.
