# Docs Contract Cleanup Status

Current-batch docs contracts for Issues 591-600 are blocking.

Historical docs-contract failures that predate this repair batch are scoped by `docs/verification/docs-contract-scope-policy.json` as non-blocking backlog. They remain visible in gate output and require follow-up cleanup before the policy expires.

This policy does not permit a clean GO when any required current-batch gate fails.
